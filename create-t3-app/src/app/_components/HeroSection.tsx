import React from "react";
import Button from "./Button";

const HeroSection = () => {
  return (
    <section>
      <h1>Hero Section</h1>
      <Button>Hero Button</Button>
    </section>
  );
};

export default HeroSection;
